fastapi
google-genai
pinecone
google-cloud-storage
pypdf
python-multipart
python-dotenv
groq
networkx
requests
beautifulsoup4
tqdm
supabase
PyJWT
stripe
twilio
# flashrank removed — pulls 250MB+ (tokenizers, onnxruntime) and pushed Vercel
# bundle over the 500MB Lambda limit. Cross-encoder rerank is gracefully
# skipped when the import fails (see api/index.py _get_rerank_fn).
